fn main() {
    let x: i32;
    x = 512;
    println!("Hello, world! x = {}", x);

    let touple: (i32, i32, i32) = (1, 2, 3);
    let (a, b, c) = touple;

    println!("a = {}, b = {}, c = {}", a, b, c);
}
